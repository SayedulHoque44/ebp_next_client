import Propile from "@/components/features/Propile/Propile";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Profile | Easy Bangla Patente",
  description: "Propile page",
};

const PropilePage = () => {
  return <Propile />;
};

export default PropilePage;
