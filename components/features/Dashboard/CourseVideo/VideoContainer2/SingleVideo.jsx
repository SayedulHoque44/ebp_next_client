import React from "react";
import { useLocation } from "react-router-dom";

const SingleVideo = ({}) => {
  const { state } = useLocation();
  //console.log(state);
  return <div>Playerd</div>;
};

export default SingleVideo;
